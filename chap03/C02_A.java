package DoItAlgorithm.chap03;
//자연스러운 정렬을 하려면 다음과 같은 방법으로 클래스를 정의합니다. 

class C02_A Comparable<A>{ //Comparable<C02_A>인터페이스 구현
	//필드 , 메서드 등
	
	public int compareTo(A c) { // compareTo메서드 
		//this가 c보다 크면 양의 값 반환
		//this가 c보다 작으면 음의 값 반환
		//this가 c와 같으면 0 반환
		
	}

	public boolean equals(Object c) { // equals 메서드 
		//this가 c와 같으면 true반환
		//this가 c와 같지 않으면 false반환 
	}
}
